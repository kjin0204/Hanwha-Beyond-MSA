server:
  port : 8000
spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: 1st-service-get
          #          uri: http://localhost:8111/
          # ??? ? ???? ???(?, application name? ??(??? ??? ????? ?? ?? ??? ??))
          uri: lb://1ST-SERVICE
          # ???? ??? ??? ?? ???? ???(??? ???)
          predicates:
            - Path=/first-service/**
          # ?? ??? ? ???? ???? /first-service?? ???? ???? ?? ????? ???? ? + ?? ??
          # ???? built in ?? + custom ??(feat. spring security) ?? ??
          filters:
            - RewritePath=/first-service/(?<segment>.*), /$\{segment}
            - AddRequestHeader=first-request, firest-request-header
            - AddResponseHeader=first-response, first-response-header
        - id: 1st-service-get
          uri: lb://2ND-SERVICE
          predicates:
            - Path=/second-service/**
          filters:
            - RewritePath=/second-service/(?<segment>.*), /$\{segment}
# gateway? routes ??? ?? ??(? ??? http method ?? ??)? ?? ???? ??
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:7861/eureka

